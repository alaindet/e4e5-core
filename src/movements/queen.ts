import { BoardSquare, BoardState } from '../board';
import { PlacedPiece } from '../piece';

export const canQueenMoveTo = (
  piece: PlacedPiece,
  square: BoardSquare,
  board: BoardState,
): boolean => {
  // TODO
  return false;
};
