import { BoardSquare, BoardState } from '../board';
import { PlacedPiece } from '../piece';

export const canRookMoveTo = (
  piece: PlacedPiece,
  square: BoardSquare,
  board: BoardState,
): boolean => {
  // TODO
  return false;
};
